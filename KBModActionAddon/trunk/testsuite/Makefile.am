AUTOMAKE_OPTIONS = foreign

EXTRA_DIST = run-tests.sh

TESTS = run-tests.sh

#TESTS_ENVIRONMENT = EXAMPLES=$(top_srcdir)/examples PARAMETERS='-s -p $(top_srcdir)/src/.libs'
TESTS_ENVIRONMENT = EXAMPLES=$(top_srcdir)/examples PARAMETERS='-s'

